<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>dev.openfeature.contrib</groupId>
        <artifactId>parent</artifactId>
        <version>[1.0,2.0)</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <groupId>dev.openfeature.contrib.tools</groupId>
    <artifactId>provider-tck</artifactId>
    <version>0.0.1</version> <!--x-release-please-version -->

    <properties>
        <module-name>${groupId}.providertck</module-name>

        <!--
            THE CONFORMANCE ARTIFACTS THIS BUILD CARRIES
            ============================================
            A conformance report has to say which questions were asked, not only what the answers
            were, so it records the open-feature/spec commit the packaged Gherkin, flag set and
            control API came from. It is filtered into
            src/main/resources-filtered/.../provider-tck-build.properties and packaged in the JAR,
            because the repository it came from is not.

            The executed Gherkin no longer rests on this pin alone: the results stream carries the
            `source` of every feature that ran, so a consumer can diff what executed against what
            this revision contains. The pin is what identifies the two artifacts the stream does not
            carry — flags/canonical-flags.json and openapi/control-api.yaml.

            It is pinned here rather than read from git at build time for one reason: unlike the Go
            TCK, this module has no spec submodule. The artifacts under src/main/resources are
            vendored copies — see the NOTE below on where they should live — so there is nothing for
            the build to interrogate. When the submodule arrives, this value is generated from it
            instead and nothing else changes; the properties file keeps its name and the report keeps
            its shape.

            Checkable rather than merely asserted, which is what makes a hand-maintained pin
            tolerable in the meantime. From a checkout of open-feature/spec:

              git rev-parse <revision>
              diff -r <spec>/specification/assets/provider-tck/gherkin src/main/resources/features

            Verified byte for byte against dc4d7ae8 at the time of writing, modulo line endings:
            this repository is checked out with core.autocrlf on Windows.
        -->
        <provider-tck.spec.revision>dc4d7ae8df1c664f82a4adf46cd43812980c0da3</provider-tck.spec.revision>

        <assertj.version>3.27.7</assertj.version>
        <awaitility.version>4.3.0</awaitility.version>
        <jackson-databind.version>2.22.1</jackson-databind.version>
        <slf4j.version>2.0.17</slf4j.version>
        <testcontainers.version>2.0.4</testcontainers.version>
        <opentest4j.version>1.3.0</opentest4j.version>
    </properties>

    <name>provider-tck</name>
    <description>
        Language-agnostic conformance test suite (TCK) for OpenFeature providers.
        Bundles the canonical Gherkin feature files, Cucumber step definitions and an
        abstract JUnit Platform Suite base class that owns the full test lifecycle:
        starting the vendor-supplied Docker Compose stack, discovering dynamically
        mapped ports, driving the standardised control API and awaiting provider
        events. Provider authors implement a single factory interface.
    </description>
    <url>https://openfeature.dev</url>

    <developers>
        <developer>
            <id>aepfli</id>
            <name>Simon Schrottner</name>
            <organization>OpenFeature</organization>
            <url>https://openfeature.dev/</url>
        </developer>
    </developers>

    <!--
        NOTE ON THE SOURCE OF TRUTH FOR THE SPEC ARTIFACTS
        =================================================
        Three of the resources in src/main/resources are NOT Java artifacts. They
        are language-agnostic definitions of the provider contract, and every
        language's TCK must agree on them byte for byte or "conformance" means
        nothing:

          features/            the canonical Gherkin — the test cases themselves
          openapi/control-api.yaml   what a backend under test must expose
          flags/canonical-flags.json the flag set those test cases assume

        They currently live in this module and are packaged into the release JAR,
        so consumers load them from the classpath and need no git submodule.

        Long term all three belong in the OpenFeature *spec* repository
        (open-feature/spec), with this module acting purely as their Java
        delivery vehicle. The three travel together by necessity: a feature file
        that evaluates `boolean-flag` is meaningless without the flag definition,
        and a disconnect scenario is meaningless without the control endpoint
        that produces the disconnect.

        When that move happens the packaging mechanism does not change, only the
        origin of the files. Wire it exactly like tools/flagd-api-testkit does
        today:

          1. git submodule add https://github.com/open-feature/spec.git spec
          2. exec-maven-plugin @ initialize, running a git submodule update
          3. maven-resources-plugin @ generate-resources -> copy
             ${basedir}/spec/provider-tck/{gherkin,flags,openapi}/
             into ${basedir}/src/main/resources/{features,flags,openapi}/

        Consumers see no difference: @SelectClasspathResource("features") keeps
        working and the JAR keeps shipping all three.
    -->

    <dependencies>
        <!--
            Everything below is deliberately `compile` scope. This artifact is a test
            *library*: consumers put it on their test classpath and the abstract suite,
            the SPI and the step definitions must all resolve from it. The OpenFeature
            SDK itself is inherited from the parent POM as a `provided` version RANGE
            ([1.21.0,1.99999)) — the TCK never pins an SDK version, so adopting it can
            never force a provider to upgrade.
        -->

        <!-- Cucumber step definition annotations — compile scope so consumers can
             discover the step classes from this JAR on the glue path -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <!-- version managed by cucumber-bom in parent -->
        </dependency>

        <!-- Cucumber JUnit Platform Engine — compile scope for the GLUE_PROPERTY_NAME /
             OBJECT_FACTORY_PROPERTY_NAME constants used in AbstractProviderTckTest -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
        </dependency>

        <!--
            Cucumber core and the message types — already on the classpath as transitive
            dependencies of cucumber-junit-platform-engine, and declared here because
            ConformanceReportPlugin compiles against them directly.

            The conformance report carries its results as a Cucumber Messages stream and produces
            that stream with io.cucumber.core.plugin.MessageFormatter, which is the same class the
            built-in `message:<path>` plugin instantiates. Cucumber's own formatter rather than one
            of ours, so the results cannot drift from what the built-in message plugin writes; only
            the output path differs, because a plugin configured through an annotation cannot have a
            path derived from the run. Versions are managed by cucumber-bom in the parent POM.
        -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-core</artifactId>
            <!-- version managed by cucumber-bom in parent -->
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>messages</artifactId>
            <!-- version managed by cucumber-bom in parent -->
        </dependency>

        <!-- PicoContainer object factory — injects TckState into every step class -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-picocontainer</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- JUnit Platform Suite — compile scope so AbstractProviderTckTest can carry
             @Suite/@IncludeEngines/@SelectClasspathResource/@ConfigurationParameter -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- JUnit Platform Launcher — TckSuiteListener implements TestExecutionListener to
             report which suite is executing, which is how a provider with several transports
             gets one suite class per transport with no extra configuration -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-launcher</artifactId>
            <scope>compile</scope>
        </dependency>

        <!--
            opentest4j — TestAbortedException is how unsupported capabilities are
            reported as SKIPPED rather than silently passing.
        -->
        <dependency>
            <groupId>org.opentest4j</groupId>
            <artifactId>opentest4j</artifactId>
            <version>${opentest4j.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- Assertions used inside step definitions -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- Awaitility — event/state awaiting with provider-configurable timeouts -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>${awaitility.version}</version>
            <scope>compile</scope>
        </dependency>

        <!--
            Testcontainers — the base class owns the ComposeContainer lifecycle.
            2.x is compiled with `options.release.set(8)`, so it links cleanly under
            this repo's maven.compiler.release=11. Kept on the same major as the
            flagd provider's existing e2e suite to avoid two Testcontainers versions
            on one test classpath.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <version>${testcontainers.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- Jackson for Object-type flag value parsing -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson-databind.version}</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
    </dependencies>

    <build>
        <!--
            The conformance artifacts are copied verbatim; only the generated build-info properties
            are filtered. Filtering the feature files would be a portability hazard rather than a
            convenience: a scenario is a byte-for-byte shared definition across four languages, and
            a stray ${...} in one would silently make this language's copy different.
        -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
            </resource>
            <resource>
                <directory>src/main/resources-filtered</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

</project>
