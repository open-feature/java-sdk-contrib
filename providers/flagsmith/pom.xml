<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>dev.openfeature.contrib</groupId>
        <artifactId>parent</artifactId>
        <version>[1.0,2.0)</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <groupId>dev.openfeature.contrib.providers</groupId>
    <artifactId>flagsmith</artifactId>
    <version>0.0.13</version> <!--x-release-please-version -->

    <name>flagsmith</name>
    <description>Flagsmith provider for Java</description>
    <url>https://flagsmith.com</url>

    <developers>
        <developer>
            <id>ajhelsby</id>
            <name>Andrew Helsby</name>
            <organization>Flagsmith</organization>
            <url>https://flagsmith.com</url>
        </developer>
    </developers>

    <properties>
        <!-- Match any provider-tck version locally; CI resolves it from the reactor -->
        <provider-tck.version>[0.0.1,)</provider-tck.version>
    </properties>

    <dependencies>
        <!-- Jackson alignment, test scope only.
             provider-tck brings jackson-databind 2.22.1, while flagsmith-java-client 7.4.3 pins
             jackson-annotations and jackson-core at 2.15.2. Maven's nearest-wins then assembles
             databind 2.22 against annotations 2.15, and the Flagsmith client's ObjectMapper dies
             with ClassNotFoundException on com.fasterxml.jackson.annotation.JsonSerializeAs.

             The provider's own 35 tests pass without the TCK on the classpath, so this is a
             conflict the TCK introduces rather than a defect in the provider — worth reporting
             upstream, since any provider whose client pins older Jackson will hit it.

             Aligned DOWN to the client's 2.15.2 rather than up: jackson-annotations has no 2.22.1
             release at all (it tracks its own version line), so aligning upward needs the Jackson
             BOM, and the TCK does not appear to need anything newer. -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.15.2</version>
        </dependency>
        <dependency>
            <groupId>dev.openfeature.contrib.tools</groupId>
            <artifactId>provider-tck</artifactId>
            <version>${provider-tck.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <version>2.0.4</version>
            <scope>test</scope>
        </dependency>
        <!-- Inherits dev.openfeature.javasdk and the test dependencies from the parent pom -->
        <dependency>
            <groupId>com.flagsmith</groupId>
            <artifactId>flagsmith-java-client</artifactId>
            <version>7.4.3</version>
        </dependency>

        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
            <version>4.12.0</version>
        </dependency>

        <!-- test -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <version>4.12.0</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

</project>
